# Runtime dependencies for the bundled reference implementation.
numpy
msgpack
websockets>=13.0  # needs the websockets.asyncio.server API (added in 13.0)
Pillow             # imported as PIL in policy/web_policy.py to JPEG-encode/decode camera frames
